name: GitHub Actions Demo
on: [push]
run-name: goodbye
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
      env:
        Linux: goodbye
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      - name: good to go
        run: | 
            env
            pwd
            ls -lht
            hostname