name: GitHub Actions Demo
on: [push]
run-name: goodbye
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    container:
      image: centos:7
      env:
        Linux: goodbye
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      - name: good to go
        run: | 
            yum install -y gcc
            curl -fsS https://dlang.org/install.sh | bash -s dmd
            source ~/dlang/dmd-2.103.0/activate
            env
            pwd
            ls -lht
            hostname
            dmd hello.d
            ./hello