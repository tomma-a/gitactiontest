name: GitHub Actions Demo
on: [push]
run-name: goodbye
env:
  name: "linux"
  abc: 12
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: print env
        run: |
          echo ${{env.name}}
          echo ${{secrets.GITHUB_TOKEN}}